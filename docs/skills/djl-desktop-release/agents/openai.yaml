interface:
  display_name: "DJL Desktop Release"
  short_description: "Ship a verified DJL desktop release with an auto-incremented version"
  default_prompt: "Use $djl-desktop-release to ship it: write the release notes, run bun run ship, and monitor the release to the production gate."
